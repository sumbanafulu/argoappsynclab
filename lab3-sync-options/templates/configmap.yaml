apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-config
data:
  app.properties: |
    app.name=demo-app
    app.env=dev
    app.version=1.0.0
  welcome.txt: |
    Welcome to the demo application!
    This file is mounted from ConfigMap.
